# GitHub Setup

This guide moved to [`RELEASING.md`](RELEASING.md).

Use `RELEASING.md` for the current branch-based release flow, marketplace validation, local deployment, rollback, and the required approval gate before any push.
